# # 📘 AI/ML Prerequisites – Hands-on Notebooks  

This repo contains my learning notes and practice notebooks for essential Python libraries that form the foundation of **AI/ML research**.  
I’ve worked through a series of tasks to strengthen my data handling, numerical computing, and visualization skills.  


## ✅ Completed Tasks

### 2. NumPy (Numerical Computing)
**Goal:** Get comfortable with matrix and vector operations.  
- Explored array creation methods (`array`, `arange`, `linspace`, `zeros`, `ones`, `eye`).  
- Practiced indexing, slicing, and reshaping arrays.  
- Worked with math operations (`mean`, `std`, `sum`, `dot`, `matmul`).  
- Used NumPy’s random module (`rand`, `randn`, `randint`, `choice`).  

**Notebook:** [Numpy.ipynb](./Numpy.ipynb)  

---

### 3. Pandas (Data Handling)
**Goal:** Learn to work with real-world datasets (CSV/Excel).  
- Created and explored DataFrames using `read_csv` and `read_excel`.  
- Cleaned messy data with `dropna`, `fillna`, `drop`, and `rename`.  
- Performed indexing and slicing with `.loc` and `.iloc`.  
- Grouped data and built pivot tables to find insights.  
- Merged with additional info and exported results (`to_csv`, `to_excel`).  

**Task:** Cleaned and analyzed the Titanic dataset, then exported a summary.  
**Notebook:** [Pandas.ipynb](./Pandas.ipynb)  

---

### 4. Matplotlib (Data Visualization)
**Goal:** Build basic plots to understand data better.  
- Drew line, bar, scatter, histogram, and pie charts.  
- Customized plots with labels, titles, legends, colors, and markers.  
- Used subplots to display multiple charts together.  
- Displayed images using `imshow`.  

**Task:** Visualized trends from datasets using multiple plot types.  
**Notebook:** [MatPlotLib.ipynb](./MatPlotLib.ipynb)  

---

### 5. Seaborn (Statistical Visualization)
**Goal:** Create clear, statistical data visualizations.  
- Explored distribution plots (`histplot`, `kdeplot`).  
- Used categorical plots (`countplot`, `barplot`, `boxplot`, `violinplot`).  
- Built relational plots (`scatterplot`, `lineplot`).  
- Visualized correlations using heatmaps.  
- Used `pairplot` and `jointplot` to study feature relationships.  

**Task:** Loaded the Titanic dataset and visualized survival rates vs. age, class, and gender.  
**Notebook:** [Seaborn.ipynb](./Seaborn.ipynb)  

---

## 🌟 Why This Matters
These tasks gave me hands-on practice with the **data science stack** in Python.  
- **NumPy** → mathematical foundation.  
- **Pandas** → data wrangling.  
- **Matplotlib & Seaborn** → turning numbers into insights.  

This foundation will help me transition smoothly into **AI/ML research**, where clean data and clear insights are just as important as building models.  

